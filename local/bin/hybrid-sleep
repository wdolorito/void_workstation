#!/bin/sh

#
# ~/local/bin/hybrid-sleep
#

SUDO="$(which sudo 2>/dev/null)"
LOCK="lock_screen"
ZZZ="zzz"

if [ -z "$SUDO" ]
then
	echo "sudo command not available."
	exit 127
else
	$SUDO "$ZZZ" -H
	$LOCK
fi
